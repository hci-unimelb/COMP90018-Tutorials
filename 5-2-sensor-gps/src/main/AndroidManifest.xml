<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.COMP90018Tutorials2024">

        <!--
          Replace this with your own key from Google Cloud Console (Maps SDK for Android),
          the same "Get an API Key" step this week's slide deck links to:
          https://developers.google.com/maps/documentation/android-sdk/get-api-key
          The map fragment will fail to load tiles until this is a real key.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyDc-2ZOqTky4yWPWy5RJcBva-MH9FnMxwE" />

        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Only ever launched from MainActivity via an explicit Intent, so it doesn't need
             its own LAUNCHER entry or to be exported to other apps. -->
        <activity
            android:name=".MapActivity"
            android:exported="false" />
    </application>

</manifest>